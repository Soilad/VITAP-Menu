[project]
name = "py"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { email = "joshtgm@icloud.com" }
]
requires-python = ">=3.14"
dependencies = [
    "openpyxl>=3.1.5",
]

[project.scripts]
py = "py:main"

[build-system]
requires = ["uv_build>=0.12.5,<0.13.0"]
build-backend = "uv_build"
